<div class="t4s-section-inner t4s-container-wrap">
  <div class="t4s-container"> 
    <div class="nonstop-banner">
      <div class="nonstop-banner__container">

        <!-- Sección Izquierda: Texto y Botón -->
        <div class="nonstop-banner__left" style="background-color: {{ section.settings.bg_color }}">
          <h2 style="color: {{ section.settings.text_color }}">{{ section.settings.title }}</h2>
          <p style="color: {{ section.settings.text_color }}">{{ section.settings.subtitle }}</p>
          
        </div>

        <!-- Sección Centro: Imagen Flotante -->
        <div class="nonstop-banner__center">
          <img src="{{ section.settings.main_image | img_url: 'master' }}" alt="Main Product" class="nonstop-banner__main-image">
        </div>

        <!-- Sección Derecha: Texto en lugar de imagen -->
        <div class="nonstop-banner__right" style="background-color: {{ section.settings.bg_color }}">
          <h2 style="color: {{ section.settings.text_color }}">{{ section.settings.right_title }}</h2>
          <p style="color: {{ section.settings.text_color }}">{{ section.settings.right_subtitle }}</p>
          <a href="{{ section.settings.button_link }}" class="nonstop-banner__button" style="background-color: {{ section.settings.button_bg_color }}; color: {{ section.settings.button_text_color }}">
            {{ section.settings.button_text }}
          </a>
        </div>

      </div>
    </div>
  </div>
</div>

<style>
  .nonstop-banner {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 6rem 0; /* Reducimos el padding */
    width: 100%;
    position: relative;
  }

  .nonstop-banner__container {
    display: grid;
    grid-template-columns: 45% 45%; /* Reducimos espacio de los textos */
    gap: 5%;
    max-width: 1400px;
    width: 100%;
    align-items: center;
    position: relative;
    border-radius: 1rem;
  }

  /* Sección Izquierda (Texto y Botón) */
  .nonstop-banner__left, .nonstop-banner__right {
    background-color: #f0f0f0;
    padding: 3rem 4rem; /* Reducimos padding */
    text-align: left;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    height: 100%;
  }

  .nonstop-banner__left h2, .nonstop-banner__right h2 {
    font-size: 2.2em; /* Reducimos tamaño del título */
    font-weight: bold;
    margin-bottom: 10px;
  }

  .nonstop-banner__left p, .nonstop-banner__right p {
    font-size: 1.2em; /* Reducimos tamaño del texto */
    margin-bottom: 15px;
  }

  .nonstop-banner__button {
    display: inline-block;
    padding: 12px 22px;
    text-decoration: none;
    font-weight: bold;
    border-radius: 1rem;
    text-align: center;
    border: 2px solid black;
    font-size: 1em;
    background: black;
    color: white;
  }

  /* Imagen Flotante en el Centro (Más grande) */
  .nonstop-banner__center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 5;
    width: 18rem; /* Aumentamos el tamaño de la imagen */
    display: flex;
    justify-content: center;
    align-items: center;
    background: transparent;
  }

  .nonstop-banner__main-image {
    width: 120%; /* Aumentamos el tamaño */
    height: auto;
    object-fit: contain;
    transform: rotate(10deg);
    filter: drop-shadow(15px 15px 40px rgba(0, 0, 0, 0.4));
    animation: float 3s ease-in-out infinite;
  }

  /* Animación de Flotación */
  @keyframes float {
    0% { transform: translateY(0px) rotate(10deg); }
    50% { transform: translateY(-10px) rotate(12deg); }
    100% { transform: translateY(0px) rotate(10deg); }
  }

  /* Responsive para Pantallas Pequeñas */
  @media (max-width: 767px) {
    .nonstop-banner__container {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 0;
    }

    .nonstop-banner__left, .nonstop-banner__right {
      width: 100%;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 2rem;
    }

    .nonstop-banner__left h2, .nonstop-banner__right h2 {
      font-size: 1.8em;
    }

    .nonstop-banner__left p, .nonstop-banner__right p {
      font-size: 1em;
    }

    .nonstop-banner__button {
      padding: 10px 18px;
      font-size: 0.9em;
    }

    .nonstop-banner__center {
      width: 12rem; /* Ajustamos tamaño en móvil */
      top: 55%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .nonstop-banner {
      padding: 4rem 0;
    }
    
    .nonstop-banner__main-image {
      width: 100%;
      height: auto;
    }
  }
</style>

{% schema %}
{
  "name": "Nonstop Banner text",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#f0f0f0"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "NONSTOP COLOR"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "NEW color-matched caps and lids."
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "text",
      "id": "right_title",
      "label": "Right Title",
      "default": "New Feature"
    },
    {
      "type": "text",
      "id": "right_subtitle",
      "label": "Right Subtitle",
      "default": "A description for the right section."
    },
    {
      "type": "image_picker",
      "id": "main_image",
      "label": "Main Image"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop New Colors"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "color",
      "id": "button_bg_color",
      "label": "Button Background Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    }
  ],
  "presets": [
    {
      "name": "Nonstop Banner text",
      "category": "Custom Sections"
    }
  ]
}
{% endschema %}
