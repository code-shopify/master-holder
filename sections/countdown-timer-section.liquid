{% schema %}
{
  "name": "Countdown Frio-Caliente",
  "settings": [
    {
      "type": "range",
      "id": "cold_hours",
      "label": "Horas frías",
      "default": 50,
      "min": 1,
      "max": 100,
      "step": 1
    },
    {
      "type": "range",
      "id": "hot_hours",
      "label": "Horas calientes",
      "default": 10,
      "min": 1,
      "max": 100,
      "step": 1
    }
  ],
  "presets": [
    {
      "name": "Countdown Frio-Caliente",
      "category": "Custom"
    }
  ]
}
{% endschema %}

{% render 'countdown-timer', cold_hours: section.settings.cold_hours, hot_hours: section.settings.hot_hours %}
