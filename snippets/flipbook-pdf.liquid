<div class="flipbook-container">
  <!-- Contenedor del flipbook -->
  <div id="flipbook"></div>

  <!-- Botones de navegación -->
  <div class="flipbook-controls">
    <button id="prevPage">⬅ Anterior</button>
    <span id="pageInfo">Página <span id="currentPage">1</span> de <span id="totalPages">?</span></span>
    <button id="nextPage">Siguiente ➡</button>
  </div>

  <!-- Botón de descarga -->
  {% if page.metafields.custom.cat_logo_pdf != blank %}
    <a class="download-btn" href="{{ page.metafields.custom.cat_logo_pdf | file_url }}" download>📥 Descargar PDF</a>
  {% endif %}
</div>

<!-- Cargar jQuery -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<!-- Cargar Turn.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/turn.js/4.1.0/turn.min.js"></script>

<!-- Cargar PDF.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.10.377/pdf.min.js"></script>

<script>
jQuery(document).ready(function ($) {
  const url = "{{ page.metafields.custom.cat_logo_pdf | file_url }}";

  if (!url) {
    $("#flipbook").html("No hay catálogo disponible.");
    return;
  }

  const flipbookEl = $("#flipbook");
  let totalPages = 0;
  let renderedPages = 0;

  pdfjsLib.getDocument(url).promise.then(function (pdf) {
    totalPages = pdf.numPages;
    $("#totalPages").text(totalPages);

    // Crear páginas vacías en el flipbook
    for (let i = 0; i < totalPages; i++) {
      flipbookEl.append(`<div class="page"><canvas id="canvas${i + 1}"></canvas></div>`);
    }

    // Cargar cada página en su correspondiente canvas
    for (let i = 1; i <= totalPages; i++) {
      pdf.getPage(i).then(function (page) {
        const viewport = page.getViewport({ scale: 1.5 });
        const canvas = document.getElementById(`canvas${i}`);
        const context = canvas.getContext("2d");

        canvas.height = viewport.height;
        canvas.width = viewport.width;

        page.render({ canvasContext: context, viewport }).promise.then(() => {
          renderedPages++;
          if (renderedPages === totalPages) {
            inicializarFlipbook();
          }
        });
      });
    }
  }).catch(function (error) {
    console.error("Error al cargar el PDF:", error);
    flipbookEl.html("<p>Error al cargar el catálogo.</p>");
  });

  function inicializarFlipbook() {
    $("#flipbook").turn({
      width: 800,
      height: 600,
      autoCenter: true,
      duration: 1000
    });

    $("#prevPage").on("click", function () {
      $("#flipbook").turn("previous");
    });

    $("#nextPage").on("click", function () {
      $("#flipbook").turn("next");
    });

    $("#flipbook").bind("turned", function (event, page) {
      $("#currentPage").text(page);
    });
  }
});
</script>

<style>
/* Contenedor principal */
.flipbook-container {
  width: 100%;
  max-width: 900px;
  margin: 20px auto;
  text-align: center;
  background: #fff;
  padding: 10px;
  border-radius: 10px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  overflow: hidden;
}

/* Ajuste del flipbook */
#flipbook {
  width: 100% !important;
  max-width: 800px;
  height: 600px;
  margin: 0 auto;
}

/* Estilos para las páginas */
.page {
  background: white;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Botones de navegación */
.flipbook-controls {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10px;
  margin-top: 10px;
}

.flipbook-controls button {
  background: #ff4c4c;
  color: white;
  border: none;
  padding: 8px 15px;
  font-size: 14px;
  border-radius: 5px;
  cursor: pointer;
}

.flipbook-controls button:hover {
  background: #cc0000;
}

/* Botón de descarga */
.download-btn {
  display: inline-block;
  margin-top: 10px;
  padding: 10px 15px;
  background: #28a745;
  color: white;
  border-radius: 5px;
  text-decoration: none;
  font-weight: bold;
}

.download-btn:hover {
  background: #218838;
}
</style>
