{% if page.metafields.custom.cat_logo_pdf != blank %}
  {% assign pdf_url = page.metafields.custom.cat_logo_pdf | file_url %}
{% else %}
  {% assign pdf_url = "" %}
{% endif %}

<!-- 📌 Cargar PDF.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.10.377/pdf.min.js"></script>

<!-- 📌 Contenedor del FlipBook -->
<div class="flipbook-container">
  {% if pdf_url != "" %}
    <div id="flipbook">
      <div id="pages"></div>
    </div>
    <div class="flipbook-controls">
      <button id="prevPage">⬅ Anterior</button>
      <span id="pageInfo">Página <span id="currentPage">1</span> de <span id="totalPages">?</span></span>
      <button id="nextPage">Siguiente ➡</button>
    </div>
  {% else %}
    <p class="error-message">⚠ No se encontró ningún PDF. Verifica el metafield en Shopify.</p>
  {% endif %}
</div>

<!-- 📌 Script para Renderizar PDF y Aplicar Efecto Flip -->
<script>
document.addEventListener("DOMContentLoaded", function () {
    const url = "{{ pdf_url }}";
    const flipbookContainer = document.getElementById("pages");
    let currentPage = 1;
    let totalPages = 0;

    if (!url) {
        console.error("❌ No se encontró la URL del PDF.");
        document.getElementById("flipbook").innerHTML = "<p style='color: red;'>⚠ No hay catálogo disponible.</p>";
        return;
    }

    pdfjsLib.getDocument(url).promise.then(pdf => {
        totalPages = pdf.numPages;
        document.getElementById("totalPages").textContent = totalPages;
        loadPage(currentPage, pdf);
    });

    function loadPage(pageNum, pdf) {
        pdf.getPage(pageNum).then(page => {
            const canvas = document.createElement("canvas");
            const ctx = canvas.getContext("2d");
            const viewport = page.getViewport({ scale: 1.5 });

            canvas.width = viewport.width;
            canvas.height = viewport.height;
            page.render({ canvasContext: ctx, viewport });

            flipbookContainer.innerHTML = "";
            const pageDiv = document.createElement("div");
            pageDiv.classList.add("flip-page");
            pageDiv.appendChild(canvas);
            flipbookContainer.appendChild(pageDiv);
        });

        document.getElementById("currentPage").textContent = pageNum;
    }

    document.getElementById("prevPage").addEventListener("click", function () {
        if (currentPage > 1) {
            currentPage--;
            loadPage(currentPage, pdfjsLib.getDocument(url).promise);
        }
    });

    document.getElementById("nextPage").addEventListener("click", function () {
        if (currentPage < totalPages) {
            currentPage++;
            loadPage(currentPage, pdfjsLib.getDocument(url).promise);
        }
    });
});
</script>

<!-- 📌 Estilos para FlipBook -->
<style>
/* 📌 Contenedor principal */
.flipbook-container {
  width: 90%;
  max-width: 800px;
  margin: auto;
  text-align: center;
  position: relative;
  background: #fff;
  padding: 10px;
  border-radius: 10px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  overflow: hidden;
}

/* 📌 Páginas */
#pages {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
}

/* 📌 Animación Flip */
.flip-page {
  position: absolute;
  width: 100%;
  height: 100%;
  transition: transform 1s ease-in-out;
  transform-origin: center;
}

/* 📌 Controles */
.flipbook-controls {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
}

.flipbook-controls button {
  background: #ff4c4c;
  color: white;
  border: none;
  padding: 8px 15px;
  font-size: 14px;
  border-radius: 5px;
  cursor: pointer;
}

.flipbook-controls button:hover {
  background: #cc0000;
}
</style>
