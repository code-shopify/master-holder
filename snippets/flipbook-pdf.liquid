<div class="flipbook-container">
  <div id="flipbook"></div>

  <div class="flipbook-controls">
    <button id="prevPage">⬅ Anterior</button>
    <span id="pageInfo">Página <span id="currentPage">1</span> de <span id="totalPages">?</span></span>
    <button id="nextPage">Siguiente ➡</button>
  </div>

  {% if page.metafields.custom.cat_logo_pdf != blank %}
    <a class="download-btn" href="{{ page.metafields.custom.cat_logo_pdf | file_url }}" download>📥 Descargar PDF</a>
  {% endif %}
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/3d-flip-book@1.0.0/dist/flipbook.min.js"></script>

<script>
jQuery(document).ready(function ($) {
    var url = "{{ page.metafields.custom.cat_logo_pdf | file_url }}";

    if (!url) {
        document.getElementById("flipbook").innerHTML = "No hay catálogo disponible.";
        return;
    }

    $("#flipbook").flipBook({
        pdfUrl: url,
        lightbox: false,
        pageSize: "small",
        pageWidth: 400,
        pageHeight: 600,
        onPageChange: function (pageNum, totalPages) {
            document.getElementById("currentPage").textContent = pageNum;
            document.getElementById("totalPages").textContent = totalPages;
        },
    });

    document.getElementById("prevPage").addEventListener("click", function () {
        $("#flipbook").flipBook("prevPage");
    });

    document.getElementById("nextPage").addEventListener("click", function () {
        $("#flipbook").flipBook("nextPage");
    });
});
</script>

<style>
/* Contenedor principal */
.flipbook-container {
  width: 90%;
  max-width: 800px;
  margin: 20px auto;
  text-align: center;
  position: relative;
  background: #fff;
  padding: 10px;
  border-radius: 10px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
}

/* Ajuste del flipbook */
#flipbook {
  width: 100%;
  height: auto;
  margin: 0 auto;
}

/* Botones de navegación */
.flipbook-controls {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10px;
  margin-top: 10px;
}

.flipbook-controls button {
  background: #ff4c4c;
  color: white;
  border: none;
  padding: 8px 15px;
  font-size: 14px;
  border-radius: 5px;
  cursor: pointer;
}

.flipbook-controls button:hover {
  background: #cc0000;
}

/* Botón de descarga */
.download-btn {
  display: inline-block;
  margin-top: 10px;
  padding: 10px 15px;
  background: #28a745;
  color: white;
  border-radius: 5px;
  text-decoration: none;
  font-weight: bold;
}

.download-btn:hover {
  background: #218838;
}
</style>